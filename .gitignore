# Legacy local config (held a Mapbox token; no longer used) — never commit
config.js

# OS / editor cruft
.DS_Store
*.log

# npm (papaparse, installed for tests/parse.test.js)
node_modules/

Presto_Transaction_history.csv