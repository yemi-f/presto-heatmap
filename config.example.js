// Copy this file to `config.js` and paste in your own Mapbox **public** access token
// (it starts with "pk."). Get one at https://account.mapbox.com/access-tokens/
//
//   cp config.example.js config.js
//
// `config.js` is git-ignored so your token never gets committed. If you skip this
// step the app will show an inline field where you can paste a token instead.
//
// Recommended: a project-specific public token with default scopes
// (styles:read, styles:tiles, fonts:read) plus a URL restriction for your
// origins. See "Mapbox token setup" in README.md.
window.MAPBOX_TOKEN = "";
